# INSTRUCCIONES: INTEGRANTE 3 (USUARIOS Y PLATA)

Tú eres el último en subir. Espera a que los otros dos terminen.

### 📁 TUS ARCHIVOS (Cópialos a tu proyecto):
- **Usuarios**: `UserController.php` y sus vistas.
- **Movimientos**: `MovementController.php` y sus vistas.

### 🛠 PASOS PARA SUBIR:
1. **IMPORTANTE**: Primero escribe en tu terminal: `git pull origin main` (para bajar lo de todos).
2. Copia tus archivos de esta carpeta a las mismas carpetas de tu proyecto Laravel.
3. Escribe estos comandos:
   ```bash
   git add .
   git commit -m "Subiendo usuarios y movimientos"
   git push origin main
   ```
