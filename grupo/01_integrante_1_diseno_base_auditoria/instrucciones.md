# INSTRUCCIONES: INTEGRANTE 1 (DISEÑO Y BASE)

Tú eres el primero en subir. Tu trabajo hace que la página se vea bien.

### 📁 TUS ARCHIVOS (Cópialos a tu proyecto):
- **Diseño**: Carpeta `resources/css/app.css`
- **Menús**: Carpeta `resources/views/layouts/` (sube los 2 archivos)
- **Auditoría**: `AuditController.php` y su vista.

### 🛠 PASOS PARA SUBIR:
1. Copia tus archivos de esta carpeta a las mismas carpetas de tu proyecto Laravel.
2. Abre la terminal en la carpeta de tu proyecto.
3. Escribe estos comandos:
   ```bash
   git add .
   git commit -m "Subiendo diseño y base"
   git push origin main
   ```

**Nota**: Si el GitHub está vacío, sube primero todo el proyecto Laravel y luego estos archivos.
